# Client (`laurel-ingestor-client`)

Aquí irá el frontend (p. ej. **React + Vite**) para consumir la API en `../api`.

**Estado:** placeholder — el siguiente paso es inicializar el proyecto en esta carpeta y configurar proxy/CORS contra `http://localhost:3000` (o el `PORT` de la API).
