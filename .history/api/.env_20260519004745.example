# Puerto HTTP (opcional; por defecto 3000 en main.ts)
PORT=3000

# Si está definida, el header x-api-key debe coincidir exactamente.
# Alineá esto con VITE_INGEST_API_KEY en client/.env (si usás el dashboard local).
# INGEST_API_KEY=dev-local-key
