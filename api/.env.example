# Puerto HTTP (opcional; por defecto 3000 en main.ts)
PORT=3000

# Si está definida, el header x-api-key debe coincidir exactamente.
# Si la omitís, solo se exige que x-api-key exista y no sea vacío (dev / tests).
# INGEST_API_KEY=secreto-compartido
