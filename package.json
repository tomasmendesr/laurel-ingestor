{
  "name": "laurel-ingestor",
  "private": true,
  "description": "Monorepo: ingest API (NestJS) + web client",
  "scripts": {
    "dev:api": "npm run start:dev --prefix api",
    "build:api": "npm run build --prefix api",
    "test:api": "npm test --prefix api",
    "test:e2e:api": "npm run test:e2e --prefix api",
    "lint:api": "npm run lint --prefix api"
  }
}
